# ZuruReels 🌍

[**Live Demo**](https://zurureels.vercel.app)


ZuruReels is a dynamic travel and event discovery platform ("City Pulse") connecting users with local experiences.

## 📂 Project Structure

The project is organized into two main directories:

-   **`/frontend`**: The React application (Vite, Tailwind, Shadcn UI).
-   **`/backend`**: Database schemas (Supabase) and backend resources.

## 🚀 Getting Started

### Frontend
To work on the user interface and application logic:

1.  Navigate to the frontend directory:
    ```bash
    cd frontend
    ```
2.  Install dependencies (if not already installed):
    ```bash
    npm install
    ```
3.  Start the development server:
    ```bash
    npm run dev
    ```

### Backend
To modify the database or explore the schema:
-   See [backend/README.md](./backend/README.md) for details.
-   SQL schemas are located in `backend/schema.sql`.

## 🔑 Key Features
-   **City Pulse**: AI-powered recommendations for events, boats, and dining.
-   **Booking System**: Integrated Paystack payments with 1-click checkout ("Save Card").
-   **Authentication**: Secure Email & Google Sign-In via Supabase.
